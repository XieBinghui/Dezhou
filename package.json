{
  "name": "dezhou",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev -w @dezhou/server & npm run dev -w @dezhou/web",
    "deps:up": "docker compose up -d redis postgres",
    "deps:down": "docker compose stop redis postgres",
    "build": "npm run build -w @dezhou/shared && npm run build -w @dezhou/server && npm run build -w @dezhou/web",
    "test": "npm run test -w @dezhou/server",
    "stress:stability": "npm run stress:stability -w @dezhou/server",
    "rc:check": "npm run build && npm run test && npm run stress:stability",
    "drill:release": "bash scripts/release-drill.sh"
  }
}
